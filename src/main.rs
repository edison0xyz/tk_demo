fn main() {
    println!("This is the main binary. Use `cargo run --bin <binary_name>` to run specific binaries.");
    println!("Available binaries:");
    println!("  - create: Run with `cargo run --bin create`");
} 